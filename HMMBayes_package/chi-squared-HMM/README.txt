Run the chi-squared HMM by replacing calls to 'hmm_process_dataset' with calls to 'hmm_process_dataset_chi' using the same input parameters.

Note that the runtime of the chi-squared HMM is significantly longer than the original HMM and it should be run in parallel on a cluster.
